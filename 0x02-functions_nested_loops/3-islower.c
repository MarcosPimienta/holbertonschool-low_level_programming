#include "holberton.h"

/**
* int _islower - prints awhich is lower
* Return: Always 0.
*/

int _islower(int c)
{

    if (c > 96)
    {
        return (1);
    }
    else
    {
        return (0);
    }
}