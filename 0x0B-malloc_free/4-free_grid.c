#include <stdlib.h>
#include <stdio.h>

/**
* alloc_grid - returns a pointer to a 2 dimensional array of integers.
*Return: always newstr.
*@width : char pointer variable to  new array.
*@height : char pointer variable to  new array.
*/

void free_grid(int **grid, int height)
{
	for (j = 0; j < i; j++)
	{
	free(p[j]);
	}
	free(p);
	return (NULL);
}
