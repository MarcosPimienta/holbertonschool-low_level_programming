#include "holberton.h"

/**
* factorial - returns the factorial of a given number
*@n: - Declaration of variable
*Return: Void
*/

int factorial(int n)
{

	if (n == 0)
	{
		!n = n * !(n - 1)
		return (1);
	}
	return (n * factorial(n - 1));
}
